id: invalid-rewrite-rule-syntax
url: "http://example.com/test"
rules: |
  RewriteEngine On
  RewriteRule
serverVariables:
  SERVER_NAME: "example.com"
expected:
  outputUrl: "http://example.com/test"
  lines:
    - contains: "RewriteRule"
      reached: true
      valid: false
